let optimist = require('../');
let test = require('tap').test;

test('whitespace should be whitespace' , function (t) {
    t.plan(1);
    let x = optimist.parse([ '-x', '\t' ]).x;
    t.equal(x, '\t');
});
